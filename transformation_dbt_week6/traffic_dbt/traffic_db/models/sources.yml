version: 2

sources:
  - name: raw
    database: dbt_db
    schema: raw

    tables:
      - name: vehicle_logs
        loaded_at_field: timestamp
        freshness:
          warn_after:
            count: 1
            period: hour
          error_after:
            count: 2
            period: hour

      - name: gps_data
  
      - name: traffic_data
      
      - name: road_segments   